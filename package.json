{
  "name": "combined-job-scraper",
  "version": "0.0.1",
  "type": "module",
  "description": "An Apify actor to orchestrate multiple job scraping actors.",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "apify": "^3.0.0"
  },
  "devDependencies": {
    "@apify/eslint-config": "^0.1.3",
    "eslint": "^8.30.0"
  },
  "scripts": {
    "start": "node main.js",
    "build": "apify actor:build",
    "lint": "./node_modules/.bin/eslint ./src",
    "lint:fix": "./node_modules/.bin/eslint ./src --fix",
    "test": "echo \"Error: oops, the actor has no tests yet\" && exit 1"
  },
  "author": "Your Name <your.name@example.com>",
  "license": "ISC"
} 